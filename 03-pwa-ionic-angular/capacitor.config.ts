import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Final-Ing-Soft-Aplicada',
  webDir: 'www'
};

export default config;
